# 앞으로 무조건 GitHub로 할 것 (APK 빌드 규칙)

> 작성일: 2026-06-03
> 지시: APK 빌드는 **무조건 GitHub Actions**로 진행한다. 샌드박스에서 로컬 빌드 금지.

## 핵심 규칙

1. **APK 빌드 = GitHub Actions 전용**
   - 샌드박스(이 작업 환경)에는 Flutter SDK / Android SDK가 설치되어 있지 않다.
   - 로컬에서 `flutter build apk` 시도하지 말 것.
   - 항상 GitHub Actions의 `Build Flutter APK` (`.github/workflows/build.yml`) 워크플로우를 사용한다.

2. **빌드 트리거 방법**
   - `main` 브랜치에 `lib/**`, `android/**`, `assets/**`, `pubspec.yaml`, `pubspec.lock` 변경이 push/merge되면 자동 빌드된다.
   - 수동 트리거: GitHub → Actions → "Build Flutter APK" → `Run workflow` (workflow_dispatch 지원).

3. **빌드 워크플로우 동작 요약** (`build.yml`)
   - 버전 자동 증가 (pubspec.yaml patch +1, build +1)
   - APK 빌드 후 GitHub Release 등으로 산출물 업로드 (워크플로우 정의 참조)

4. **표준 작업 순서**
   1. 코드 수정 → 커밋 → `genspark_ai_developer` 푸시
   2. PR 생성 → `main`으로 머지
   3. 머지되면 GitHub Actions가 APK 자동 빌드
   4. Actions 결과(Release/Artifact)에서 APK 다운로드

## 절대 금지
- 기존 링고(RinGo) 핵심 동작을 테스트 목적의 변경으로 깨뜨리지 말 것.
- 로컬 샌드박스에서 APK 빌드 환경을 설치/빌드하지 말 것 (GitHub로만).
