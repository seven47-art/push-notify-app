// lib/screens/splash_screen.dart
// v3.1.9: 사용하지 않음 - main.dart SplashScreen이 직접 처리
// 이 파일은 호환성 유지를 위해 빈 stub으로 유지
import 'package:flutter/material.dart';

class SplashScreenLegacy extends StatelessWidget {
  const SplashScreenLegacy({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
